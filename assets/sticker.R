library(showtext)
library(hexSticker)

hexSticker::sticker(
  package = "",
  subplot = "assets/raw.png",
  s_y = 0.8,
  s_x = 1,
  s_width = 1.05,
  spotlight = TRUE,
  l_y = 1.3,
  l_x = 0.95,
  h_fill = "#EECE63",
  h_color = "#AD890F",
  dpi = 1200,
  filename = "assets/NuisanceParameters.png"
)
